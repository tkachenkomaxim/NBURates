
import 'dart:ui';

class AppConfig {
  static const translationPath = 'assets/translations';
  static const supportedLocales = [Locale('en', 'US'), Locale('uk', 'UA')];
  static const fallbackLocale = Locale('en', 'US');

  static const title =  'NBU Rates App';
}